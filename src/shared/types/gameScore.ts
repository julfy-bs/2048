import { TScore } from './score';

export type TGameScore = {
  score: TScore;
  bestScore: TScore;
  font: string;
  fontColor: string;
  fontSize: number;
  color: string;
  height: number;
  width: number;
}
